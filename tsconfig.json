{
	"compilerOptions": {
	  "module": "NodeNext",          // ✅ Better compatibility with ES Modules
	  "target": "ES2022",
	  "lib": ["ES2022", "DOM"],      // ✅ Add "DOM" to support browser-related APIs
	  "sourceMap": true,
	  "rootDir": "src",
	  "skipLibCheck": true,
	  "strict": true,
	  "noEmit": false,               // ✅ Allow emitting code for the extension
	  "esModuleInterop": true,
	  "resolveJsonModule": true,     // ✅ Helps with JSON imports if needed
	  "forceConsistentCasingInFileNames": true, // ✅ Prevents casing issues (important on macOS/Linux)
	  "incremental": true            // ✅ Improves performance for large projects
	},
	"include": ["src"],              // ✅ Limit scope to the source directory
	"exclude": ["node_modules", "dist"] // ✅ Avoid unnecessary files
  }
  